# Scirate-Bots
Bots for various platforms

# Discord Bot
### Installation 
Add bot token to config.py 
```
TOKEN=''
PREFIX='!?'
```
- Docker
  ```
  docker build .
  docker run -i -t <IMAGE>
  ```
- Locally 
  ```
  python3 -B index.py 
  ```
